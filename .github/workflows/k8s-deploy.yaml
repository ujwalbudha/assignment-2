name: Deploy to Kubernetes

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'app'
        type: choice
        options:
          - app

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBECONFIG }}" > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Deploy to Kuberneteshho "${{ secrets.KUBECONFIG }}" > $HOME/.kube/config
          chmod 600o "${{ secrets.KUBECONFIG }}" > $HOME/.kube/config
          chmod 600
        run: kubectl apply -k k8s/

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/api-service -n ${{ inputs.environment }} --timeout=300s
          kubectl rollout status deployment/frontend-service -n ${{ inputs.environment }} --timeout=300s
          kubectl rollout status deployment/worker-service -n ${{ inputs.environment }} --timeout=300s
